"""LangGraph chat agent + MCP (Grafana) tooling. Read-only.

Powers the conversational Discord bot and the on-demand `!investigate` deep-dive.
The deterministic alert daemon does NOT use this package.
"""
