"""Discord bot — chat with the agent, request screenshots, list incidents.

Read-only: the bot can only query metrics/logs/incidents and capture panels.
Automatic alerting continues via the webhook notifier (run alongside in `gaa bot`).
"""
